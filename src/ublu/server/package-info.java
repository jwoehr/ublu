/**
 * A server for the {@link ublu.Ublu} interpreter.
 * Most of the {@link ublu.util.Interpreter}'s functionality is accessible via
 * TCP/IP once the "server -start" command is issued.
 */
package ublu.server;
